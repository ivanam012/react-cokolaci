import React from 'react';
import {HomeContainer} from '../components/HomeElements';

const Home = () => {
    return (
        <div style={{display: 'flex', justifyContent: 'center', alignItems: 'center', height: '90vh'}}>
            <HomeContainer />
            
            
        </div>
    )
}

export default Home
